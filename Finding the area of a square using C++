#include <iostream>
#include <cmath>

int main() {
    float side, area;
    side = pow(side, 2); 
    std::cout << "Insira o valor do lado do quadrado: ";
    std::cin >> side;
    area = pow(side, 2); 
    std::cout << "A area do quadrado eh: " << area << std::endl;
    return 0; 
}
